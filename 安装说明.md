# DeepSeek Harness 桌面客户端 · 安装说明（详细版）

> 适用版本：`DeepSeek Harness Setup 1.0.3.exe`
> 最后更新：2026-08-20

本软件 = **DeepSeek Harness 桌面客户端**：Electron 壳（窗口内加载官方 Web GUI，界面与官方 100% 一致）
+ 五个能力插件（长期记忆 / 社交渠道桥接 / 自动视觉 / 侧边栏快捷入口 / 会话标题规范）+ Lucky 主脑预设。
详细能力见《[功能说明](功能说明.md)》，版本历史见《[更新说明](更新说明.md)》。

---

## 目录

1. [安装包内容](#一安装包内容)
2. [运行架构（先看懂这个）](#二运行架构先看懂这个)
3. [本机安装](#三本机安装)
4. [全新机器完整部署](#四全新机器完整部署)
5. [安装后验证清单](#五安装后验证清单)
6. [升级与重装](#六升级与重装)
7. [卸载](#七卸载)
8. [故障排查](#八故障排查)
9. [隐私与密钥说明](#九隐私与密钥说明)

---

## 一、安装包内容

| 组件 | 说明 |
|---|---|
| Electron 壳 | 主窗口：加载官方 Web GUI（`main.js`，中文注释） |
| node-runtime | 自带 Node.js 运行时（宿主进程用它启动，不依赖系统 Node） |
| plugins/social-bridge | 社交渠道桥接插件（微信 iLink 真实桥接 + 四渠道占位卡） |
| plugins/vision-router | 自动视觉插件（贴图自动转写为文字证据） |
| plugins/memory-evolve | 长期记忆插件（五轨记忆/待办/技能/记忆审查） |
| plugins/dsh-sidebar-shortcuts | 侧边栏快捷入口插件（内嵌插件市场 + 知识库含 Office/PDF 预览 + 关于） |
| plugins/session-title-suffix | 会话标题规范插件（「任务描述(AI名)」统一格式） |

**隐私声明**：安装包经全量扫描，不含任何 API 密钥、个人身份数据与真实凭据值。
所有密钥与用户数据保存在用户数据目录（见第九节），与安装包完全隔离。

## 二、运行架构（先看懂这个）

桌面窗口只是"壳"，真正的 AI 宿主来自 DeepSeek Harness 运行时：

```
┌─────────────────────────────┐
│  DeepSeek Harness.exe（壳） │   ← 安装包提供
│  窗口 = 官方 Web GUI 页面    │
└──────────────┬──────────────┘
               │ 启动时：① 探测官方实例(3080)，有则直接接入
               │        ② 没有则用 node-runtime 拉起仓库 CLI（默认 13800）
┌──────────────▼──────────────┐
│  dsh web 宿主进程            │   ← 来自 DSH 源码仓库（构建产物）
│  （模型路由/工具/会话/记忆）  │
└──────────────┬──────────────┘
               │ 共用用户数据目录 .dsh（会话/设置/记忆/插件配置/凭据）
┌──────────────▼──────────────┐
│  插件（profile 注册）        │   ← 记忆体 / 微信桥 / 视觉路由
└─────────────────────────────┘
```

要点：
- **壳与宿主分离**：升级壳不需要动宿主，重启宿主不需要重装壳
- **数据单一来源**：所有会话、记忆、配置都在用户数据目录，重装软件不丢数据
- **端口**：复用官方实例走 3080；自启宿主默认 13800（环境变量 `DSH_WEB_PORT` 可改）

## 三、本机安装

> 前置：本机已具备运行依赖（DSH 仓库运行时 + 用户数据目录 + 插件注册，见第四节）。
> 若只是本机重装/升级，直接走本节即可。

### 3.1 图形安装

1. 双击 `DeepSeek Harness Setup 1.0.3.exe`
2. 选择安装目录（默认 `%LOCALAPPDATA%\Programs\DeepSeek Harness`）
3. 勾选「创建桌面快捷方式」（默认已勾）
4. 完成 → 自动启动

### 3.2 静默安装（命令行 / 脚本化）

```powershell
& "<安装包路径>\DeepSeek Harness Setup 1.0.3.exe" /S
```

静默安装走默认目录、默认创建快捷方式；安装完成后如需立即启动：

```powershell
Start-Process "$env:LOCALAPPDATA%\Programs\DeepSeek Harness\DeepSeek Harness.exe"
```

### 3.3 安装后检查

- 桌面出现「DeepSeek Harness」快捷方式（官方鱼形 logo）
- 开始菜单 → DeepSeek Harness
- 启动后窗口标题为 `DeepSeek Harness`

## 四、全新机器完整部署

> 本安装包覆盖"壳 + 插件代码"。全新机器还需以下三部分（**当前安装包不包含**）：

### 4.1 DSH 源码仓库运行时

```powershell
# 1. 获取仓库（以 <仓库路径> 代指本机仓库根，下同）
git clone https://github.com/deepseek-ai/deepseek-harness.git <仓库路径>
cd <仓库路径>
pnpm install          # 需要 pnpm（建议 10+）

# 2. 构建产物（CLI + Web 前端）
pnpm run build
```

构建产物：`<仓库路径>\apps\cli\lib\bin.js` 与 `apps\web\dist`。

壳的仓库定位规则（`main.js` 内置）：环境变量 `DSH_HARNESS_REPO` → asar 上级（开发布局下为仓库根）。
非默认路径时设置系统环境变量即可：

```powershell
[Environment]::SetEnvironmentVariable('DSH_HARNESS_REPO', '<仓库路径>', 'User')
```

### 4.2 用户数据目录（profile + 插件注册）

```powershell
# 1. 初始化 web profile（首次启动 dsh web 会自动完成，也可手动触发）
dsh plugin --profile web add link:<桌面工程>\social-bridge
dsh plugin --profile web add link:<桌面工程>\vision-router
dsh plugin --profile web add link:<桌面工程>\memory-evolve
dsh plugin --profile web add link:<桌面工程>\dsh-sidebar-shortcuts

# 2. 配置第二大脑（Obsidian 库）路径（机器私有，写在 profile 补丁里）
#    profiles\web\cordis.patch.yml 追加：
#    - id: dsh-sidebar-shortcuts
#      config:
#        vault: '<知识库路径>'

# 3. 启动一次验证
dsh web
```

安装包内的插件副本位于 `%LOCALAPPDATA%\Programs\DeepSeek Harness\resources\plugins\`，
可解包后按上述 link 命令注册（或直接用本机开发目录）。

### 4.3 凭据（需要哪些）

| 用途 | 环境变量 / 文件 | 说明 |
|---|---|---|
| 会话模型 | `DEEPSEEK_API_KEY`（存 `.dsh\.credentials.yaml`） | 官方 API |
| 自动视觉 | `ARK_API_KEY`（同文件） | 火山方舟，模型 `doubao-seed-2-1-pro`（需在方舟控制台开通） |
| 微信桥 | 扫码登录自动写入 `.dsh\storages\social-bridge\config.json` | 无需手动配 |

## 五、安装后验证清单

| # | 检查项 | 预期 |
|---|---|---|
| 1 | 双击快捷方式 | 窗口出现，标题 `DeepSeek Harness` |
| 2 | 设置 → Agent 预设 | 能看到 `Lucky`（默认新会话使用） |
| 3 | 设置 → 社交渠道桥接 | 五张卡片；微信卡点「登录」弹二维码，扫码后"已连接" |
| 4 | 微信发一条消息 | 自动进入所选会话，回复自动发回微信 |
| 5 | 微信发一张图片 | 图片自动保存并被视觉理解，回复基于图片内容 |
| 6 | 对话框粘贴一张图 | 自动转写（约 6 秒）后主模型基于图片内容作答 |
| 7 | 左下角「插件市场」 | 软件内打开市场面板：搜索/分类/一键安装/翻译可用 |
| 8 | 左下角「知识库」 | 软件内打开库面板：目录树 + 笔记预览（图片/视频内嵌）+ Office/PDF 预览（docx 排版、xlsx 表格、pptx 幻灯片） |
| 9 | 设置 → 关于 | 显示版本号 `v1.0.3`、版权归属 Ambin.D |
| 10 | 会话页记忆标签 | 五轨记忆可用；重启后凭据/游标/记忆全部保留 |
| 11 | 关闭窗口再开 | 宿主任意重启，会话在左侧列表 resume |

## 六、升级与重装

| 场景 | 操作 | 数据影响 |
|---|---|---|
| 升级壳/插件 | 直接运行新安装包（覆盖安装） | 无（用户数据目录独立） |
| 插件代码更新 | 更新桌面工程目录后重启宿主 | 无 |
| 重装整个软件 | 卸载 → 重装 | 无（`.dsh` 与仓库不被卸载程序删除） |
| 更新 memory-evolve | 上游出新版后，同步 `desktop\memory-evolve` 副本 + 重新打包 | 无 |

## 七、卸载

1. `%LOCALAPPDATA%\Programs\DeepSeek Harness\Uninstall DeepSeek Harness.exe`
   或「设置 → 应用 → DeepSeek Harness → 卸载」
2. 卸载只移除软件本体；以下**不会被删除**（需要时手动清理）：
   - 用户数据目录 `<用户目录>\.dsh`（会话/记忆/凭据）
   - DSH 仓库运行时
   - 桌面工程目录

## 八、故障排查

| 现象 | 原因 | 处理 |
|---|---|---|
| 启动弹「宿主未就绪」 | 仓库未构建 / 路径不对 | 确认 `apps\cli\lib\bin.js` 存在；否则 `pnpm run build`；或设 `DSH_HARNESS_REPO` |
| 窗口空白/连不上 | 宿主没起来或端口冲突 | 关窗口重开；查 13800/3080 占用；看控制台输出 |
| 设置页没有插件区块 | 宿主未重启 / 插件未注册 | 关窗口重开；核对 `.dsh\profiles\web\package.json` 的 bundles |
| 微信桥状态错误 | 会话过期 / 网络问题 | 卡片点「检查连接」看原因；过期则重新扫码 |
| 贴图没被"看懂" | 视觉模型未开通 / 凭据缺失 | 确认方舟账号已开通 `doubao-seed-2-1-pro`；`ARK_API_KEY` 已配置 |
| 记忆不生效 | 宿主旧进程缓存 | 完全重启宿主后新会话验证 |

## 九、隐私与密钥说明

**所有密钥与个人数据均在安装包之外**，位置清单：

| 文件 | 内容 |
|---|---|
| `<用户目录>\.dsh\.credentials.yaml` | DEEPSEEK_API_KEY、ARK_API_KEY |
| `<用户目录>\.dsh\storages\social-bridge\config.json` | 微信 botToken/accountId/会话令牌表 |
| `<用户目录>\.dsh\sessions / memories` | 会话记录、长期记忆 |
| 知识库 vault | 个人知识库（按隐私铁律以"隐私项"占位） |

原则：密钥只存用户数据目录、代码零硬编码；分享/发布任何工程文件前，
先做特征词扫描（密钥值形态、个人身份信息），确保不随代码外泄。
