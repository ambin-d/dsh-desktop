# DeepSeek Harness 桌面客户端 · 功能说明

软件 = 官方 DeepSeek Harness Web GUI（原汁原味）+ 桌面壳 + 四个能力插件 + Lucky 主脑预设。
桌面窗口里的界面就是官方页面本身：颜色、文字、布局、快捷键与官方 Web 版 100% 一致。

---

## 一、桌面壳

| 能力 | 说明 |
|---|---|
| 会话接入 | 窗口自动接入官方宿主：有实例直接显示当前会话；没有则自动拉起一个（13800 端口） |
| 数据共用 | 与命令行 `dsh web` 共用同一 `.dsh` 目录：会话、设置、记忆、插件全部互通 |
| 快捷键 | `F12`/`Ctrl+Shift+I` 开发者工具、`Ctrl+R` 刷新、`F11` 全屏——与浏览器一致 |
| 下载 | 弹系统"另存为"对话框 |
| 外链 | 一律交系统浏览器打开 |
| 单实例 | 重复双击图标聚焦已有窗口，不会开两个宿主 |
| 生命周期 | 关窗口只退壳；自己拉起的宿主随之回收，接入的官方实例不受影响 |

## 二、长期记忆（dsh-memory-evolve 插件）

跨会话不丢上下文，五轨记忆：

| 轨 | 说明 |
|---|---|
| 用户档案 / 全局事实 | 低频稳定内容**自动注入**每个新会话（写入需你确认） |
| 项目关键记忆 | 按 git 分支隔离；说"记一下：××"即提交待确认建议，确认后注入 |
| 项目日志 / 每日日志 | 每轮自动记录进展，按需读取 |
| 待办（四轨） | 生活/工作/项目/每日；"记住，周五前××"自动落待办，到期提醒 |
| 记忆审查 | 每 10 轮自动提炼值得记住的信息，交你确认 |

入口：会话页「记忆 · 技能 · 待办」标签 + 设置「Memory Evolve 设置」。记忆写入前须你确认，不私自落笔。

## 三、社交渠道桥接（dsh-social-bridge 插件）

设置页「社交渠道桥接」分组，五张卡片：

| 渠道 | 状态 |
|---|---|
| **微信（iLink/OpenClaw 通道）** | ✅ 真实桥接 |
| Telegram / 飞书 / 钉钉 / WhatsApp | 占位卡片（字段可填，暂不接真实 SDK） |

**微信桥（核心能力）**：

- **登录**：卡片点「登录」→ 弹二维码 → 手机微信扫码 → 状态变"已连接"并显示账号
- **收发**：微信发消息 → 自动进入你选的会话（带【微信消息】标注）；你的回复在回合结束后自动发回微信
- **媒体收发**：微信发图片 → 自动保存并经视觉链路理解（AI 直接基于图片作答）；发文件/视频 → 自动保存到媒体目录并把路径告知会话；发语音 → 自动转写为文字（服务端转写优先 + 本地 ASR 兜底）
- **署名**：平时私聊回复不署名；仅当多个 AI 协作同一账号时才设置署名区分说话人
- **切换会话**：微信上回复「列出会话」「切换会话 N」「新会话」即可管理接收会话，不用碰设置页
- **持久化**：凭据、游标、会话令牌表全部落盘，重启自动恢复、续读不丢消息
- **故障可见**：会话过期/发送失败原因直接显示在卡片上；回合报错会把原因自动发回微信，不静默丢消息
- **范围**：仅 1 对 1 私聊（iLink 通道不支持群聊）

## 四、自动视觉（dsh-vision-router 插件）

**零人工切换**：你在对话框粘贴/拖入图片后——

```
图片 → 自动转文字证据（视觉模型，关思考约 6 秒/张）→ 主模型基于证据作答
```

- 会话模型、人格、记忆、推理全部不变，只是"看"这一步自动发生
- 同一张图在后续回合自动复用转写结果（不重复耗时）
- 转写失败会以文字说明原因，绝不吞掉你的输入
- 备用路线：设置 → 模型 可手动切「火山方舟 → 豆包 Seed 2.1 Pro（视觉）」原生视觉模型

## 五、侧边栏快捷入口（dsh-sidebar-shortcuts 插件）

桌面客户端左下角（设置旁）两个入口按钮，全部在软件窗口内完成，不用离开客户端：

### 插件市场（内嵌）

- **数据源**：Oh-My-DSH 社区目录，1700+ 插件、11 大分类，每小时自动刷新缓存
- **中文搜索** + 分类筛选；每条插件带类型/语言/活跃度/Star 标签与中文描述
- **一键安装**：点每条插件的「一键安装」走官方 `dsh plugin add` 同一条通道，装完提示重启宿主生效
- **翻译按钮**：英文描述点「翻译」就地变中文（译文/原文可切换，按需单条翻译）
- 「已装插件」页签查看本机已装列表

### 知识库（内嵌）

- **目录树**：左侧第二大脑目录树（懒加载、可折叠、排除隐藏目录）+ 关键词过滤
- **Markdown 预览**：右侧只读渲染标题/列表/代码块/表格/引用
- **看图/看视频**：文章里的 `![[图片]]` 内嵌显示（点击放大看原图）、`![[视频]]` 内嵌播放器（支持拖进度条）、`![[音频]]` 内嵌播放
- **只读纪律**：本插件只读取浏览，绝不提供写入/编辑/删除，第二大脑原样不动
- 右上角保留「用 Obsidian 打开 / 打开库文件夹」补充入口

### 关于（设置页）

设置 → 关于：显示桌面客户端版本号、版权归属（Ambin.D）与联系邮箱。

## 六、Lucky 主脑预设

自建 Agent 预设（`lucky`），默认用于新会话：

- **人格**：干练直接、有主见、忠诚优先；像老朋友发微信，不写报告腔
- **五条铁律**：隐私（个人档案一律"隐私项"占位）/ 落地（交付带证据）/ 署名（对外消息署名，微信私聊例外）/ 记忆（写入须确认）/ 停手（说停就停）
- **知识库**：知识总库在 `<知识库路径>`（Obsidian 库）——查规则翻 00、项目看 05、复盘台账在 06、协作规则 07；案例类产出当场归档
- 新会话即 Lucky；旧会话保持原预设不变

## 七、模型

| 项 | 配置 |
|---|---|
| 默认会话模型 | DeepSeek-V4-Flash（官方） |
| 视觉模型 | 火山方舟 `doubao-seed-2-1-pro`（自动视觉转写用；也可手动选择） |
| 翻译模型 | 阿里云百炼 qwen-turbo（插件市场英文描述翻译用） |
| 密钥 | 全部存于 `.dsh\.credentials.yaml`，代码零硬编码 |

---

## 版本信息

当前版本与更新内容见《[更新说明](更新说明.md)》；设置 → 关于 可查看版本号与版权归属。
