# DeepSeek Harness 桌面客户端 · 更新说明

> 版本号规则：每次封装安装包，版本号必须递增，文件名随版本号（`DeepSeek Harness Setup <版本号>.exe`）。

---

## v1.0.3（2026-08-20）

### 知识库新增：Office/WPS 文档预览

知识库（第二大脑）目录树现在可以直接预览办公文档，不再只支持 Markdown 与媒体：

| 文件类型 | 树内图标 | 预览效果 |
|---|---|---|
| Word `.docx` | 📝 | 完整排版渲染（标题/表格/图片） |
| Excel `.xlsx` / `.xls` | 📊 | 表格预览，多工作表标签切换 |
| PowerPoint `.pptx` | 📽️ | 逐页幻灯片卡片（文本 + 内嵌图片） |
| PDF | 📕 | 浏览器原生渲染，可滚动翻页 |
| WPS 旧版 `.wps`/`.et`/`.dps`、`.doc`/`.ppt` | 🗎 | 提示说明 + 一键「用默认程序打开」 |

- 大文件上限 10MB；加密/损坏文档有友好提示
- 全程只读预览，绝不写入/修改任何文件
- WPS 旧版私有格式不支持内嵌解析，在 WPS 中「另存为 docx/xlsx/pptx」后即可直接预览

### 插件构建升级（dsh-sidebar-shortcuts）

- 插件宿主改为 esbuild 构建，Office 解析依赖（mammoth/xlsx/jszip）全部内联，安装包自足——在任何环境装上即可用，不再依赖外部依赖目录

---

## v1.0.2（2026-08-19）

### 会话标题规范插件（dsh-session-title-suffix）

- 所有会话标题自动统一为「任务描述(AI名)」格式（半角括号）
- 按 Agent 预设映射 AI 名（Lucky / 当当 / 当当秘书 / 丁丁），会话列表一目了然

---

## v1.0.1（2026-08-18）

### 新增：侧边栏快捷入口（dsh-sidebar-shortcuts 插件）

桌面客户端左下角新增两个入口按钮：

1. **插件市场（内嵌）**
   - 软件内打开大面板，全程不离开客户端窗口
   - 数据源：Oh-My-DSH 社区目录（1700+ 插件、11 大分类、中文描述/Star/活跃度/语言标签，每小时自动刷新缓存）
   - 中文搜索（插件名/描述/类型/语言）+ 分类筛选
   - 每条插件「一键安装」（走官方 dsh plugin add 同一条通道，安装成功提示重启宿主生效）
   - 英文描述点「翻译」就地变中文（译文/原文可切换）
   - 「已装插件」页签保留原已装列表功能

2. **知识库（内嵌）**
   - 左侧第二大脑目录树（懒加载、可折叠、排除隐藏目录）+ 右侧 Markdown 只读预览
   - 关键词过滤当前目录
   - **看图/看视频**：文章里的 `![[图片]]` 内嵌显示（点击放大看原图）、`![[视频]]` 内嵌播放器（支持拖进度条）、`![[音频]]` 内嵌播放
   - 右上角保留「用 Obsidian 打开 / 打开库文件夹」补充入口
   - 只读纪律：本插件只读取浏览，绝不提供写入/编辑/删除

### 升级：微信桥（dsh-social-bridge）

- **图片收发**：微信发图 → 自动保存并交给视觉链路理解，AI 直接基于图片内容作答
- **文件/视频收发**：自动保存到媒体目录并把路径告知会话，AI 可直接读取处理
- 会话管理：微信「新会话」自动带默认模型与预设、挂接工作区；会话列表带名称兜底（不再全是"未命名会话"）；回合报错会把原因自动发回微信，不再石沉大海

### 新增：设置页「关于」

- 设置 → 关于：显示桌面客户端版本号、版权归属（Ambin.D）与联系邮箱
- 版本号随每次封装自动递增

### 其他

- 安装包新增随附 dsh-sidebar-shortcuts 插件
- 视觉/记忆/预设等原有能力保持不变

---

## v1.0.0（2026-08-17）

- 首个发布版本：Electron 壳（窗口内加载官方 Web GUI，界面与官方 100% 一致）
- 随附三插件：长期记忆（dsh-memory-evolve）/ 社交渠道桥接（dsh-social-bridge）/ 自动视觉（dsh-vision-router）
- Lucky 主脑预设
